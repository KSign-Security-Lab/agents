{
  "name": "agents",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  }
}
