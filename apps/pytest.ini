[pytest]
# pytest-asyncio 0.25 warns when this is unset and will change its default; the
# tests here need no shared loop, so a fresh one per test is the safe pin.
asyncio_default_fixture_loop_scope = function
